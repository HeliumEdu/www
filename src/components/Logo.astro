---
import { Image } from 'astro:assets';
import { SITE } from 'astrowind:config';
import logo from '~/assets/images/helium-logo.png';
---

<Image src={logo} alt={SITE?.name} height={32} densities={[1, 2, 3]} class="h-8 w-auto" />
